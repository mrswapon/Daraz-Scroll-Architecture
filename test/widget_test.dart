import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — real tests would mock ApiService and test BLoC states.
    expect(1 + 1, equals(2));
  });
}
